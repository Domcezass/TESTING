-- Script to Tamper with Pet Chances in Saber Simulator (Executor Test)

-- Reference to ReplicatedStorage and the Eggs data
local petsFolder = game:GetService("ReplicatedStorage"):WaitForChild("Eggs"):WaitForChild("EH Egg"):WaitForChild("Pets")

-- Check if the Easter Death pet exists
local easterDeathEgg = petsFolder:WaitForChild("Easter Death")

-- Ensure the pet chance is a NumberValue and modify it
if easterDeathEgg and easterDeathEgg:IsA("NumberValue") then
    -- Tampering with the chance by increasing it significantly
    easterDeathEgg.Value = 1000  -- Modify this value to make the chance extremely high
    print("Easter Death chance tampered with!")
else
    print("Easter Death pet not found!")
end

-- Optional: You can also trigger the hatching process if needed:
local args = {
    [1] = easterDeathEgg,  -- Selecting the Easter Death egg
    [2] = 1  -- Number of eggs to hatch
}

game:GetService("ReplicatedStorage"):WaitForChild("Events"):WaitForChild("HatchEggs"):InvokeServer(unpack(args))
print("Triggered egg hatching!")
